# UI States

For every meaningful UI feature, consider:

- loading state
- empty state
- error state
- success state
- disabled state
- validation state
- mobile layout
- desktop layout

Do not design only the happy path.

Interactive components should define what users see before data loads, when no data exists, when an action fails, and when an action is unavailable.

