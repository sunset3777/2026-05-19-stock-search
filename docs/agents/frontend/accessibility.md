# Accessibility

For interactive UI, consider:

- semantic HTML
- keyboard navigation
- visible focus states
- form labels
- button names
- dialog focus management
- color contrast
- disabled and loading behavior

Use ARIA only when semantic HTML is not enough.

Custom controls must support keyboard interaction and clear accessible names.

